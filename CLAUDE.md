# Claude Instructions — baby-tracker-feedback

## Commits

- **Never** add `Co-Authored-By` trailers to any commit message in this repo.
- Every commit must use author `Olesia Serban <OlesiaSerban@gmail.com>`.
- Always pass `-c user.email="OlesiaSerban@gmail.com" -c user.name="Olesia Serban"` when running `git commit`, or verify `git config user.email` returns `OlesiaSerban@gmail.com` before committing.
